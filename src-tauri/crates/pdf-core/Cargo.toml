[package]
name = "pdf-core"
version = "0.1.0"
edition = "2021"

[dependencies]
layout-core = { path = "../layout-core" }
lopdf = "0.35"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
